<li>
	{{burger}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}DEVOUR TIME!{{else}}REVERSE EAT!{{/if}}
	</button>
	<button class="delete-burger" data-id="{{id}}">
		DELETE BURGER
	</button>

</li>